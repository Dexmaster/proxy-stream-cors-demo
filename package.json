{
  "name": "proxy-stream-cors-demo",
  "version": "1.0.1",
  "description": "Demo of proxying data through http-proxy-stream with CORS allowed (Node.js + Express + http-proxy-stream + CORS middleware)",
  "main": "express-app.js",
  "scripts": {
    "start": "node src/express-app.js",
    "lint": "eslint ./ --cache --ignore-pattern .gitignore",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrii Malytskyi <a.malytskyi@gmail.com> (https://github.com/Dexmaster)",
  "license": "MIT",
  "dependencies": {
    "connect-timeout": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "http-proxy-stream": "^1.3.4"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": [
    "lint"
  ]
}
